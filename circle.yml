test:
  pre:
    - ./gradlew wrapper
  override:
    - ./gradlew test
    - mkdir -p $CIRCLE_TEST_REPORTS/junit
    - cp -R build/test-results/* $CIRCLE_TEST_REPORTS/junit
  post:
    - ./gradlew distZip

general:
  artifacts:
    - "build/distributions"
    - "build/test-results"