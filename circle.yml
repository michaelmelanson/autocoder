test:
  override:
    - ./gradlew test
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit
    - cp -R build/test-results/* $CIRCLE_TEST_REPORTS/junit
    - ./gradlew distZip

general:
  artifacts:
    - "build/distributions"
    - "build/test-results"